[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "geo3dcull"
version = "1.0.0"
description = "Geo3DCull: GPU/CPU-accelerated 3D mesh visibility culling toolkit"
readme = "README.md"
license = {text = "CC-BY-NC-4.0"}
authors = [
    {name = "Stefano Moriconi"},
]
keywords = ["3d", "mesh", "culling", "visibility", "geometry", "cuda", "openmp", "geo3dcull"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: Free for non-commercial use",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
    "Operating System :: OS Independent",
]
requires-python = ">=3.8"

dependencies = [
    "numpy>=1.20",
    "scipy>=1.7",
]

[project.optional-dependencies]
# Full feature set (visualization + decimation)
full = [
    "matplotlib>=3.4",
    "point-cloud-utils>=0.9",
]
# Only visualization (no decimation)
viz = [
    "matplotlib>=3.4",
]
# For development
dev = [
    "pytest>=7.0",
    "matplotlib>=3.4",
    "point-cloud-utils>=0.9",
]

[project.urls]
Homepage = "https://github.com/stefanomoriconi/Geo3DCull"
Documentation = "https://github.com/stefanomoriconi/Geo3DCull#readme"
"Source Code" = "https://github.com/stefanomoriconi/Geo3DCull"

[tool.setuptools.packages.find]
where = ["."]
include = ["geo3dcull*"]
